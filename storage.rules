rules_version = '2';

// ATENÇÃO:
// Este arquivo estava ausente no repositório, mas é referenciado em `firebase.json`.
// Revise essas regras antes de fazer deploy em produção.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}


